#include "debug.h"
#include "libft/libft.h"
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

int main ()
{
    char s1[100] = "";
    char *s2 = NULL;
    char *temp;
    char *temp0;
    int i = 0;

    // temp = ft_strdup(s2);
    temp0 = strdup(s2);
    // fprintf(stderr, "temp\t: %s\n", temp);
    fprintf(stderr, "temp0\t: %s\n", temp0);
}